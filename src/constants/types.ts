export interface PostData {
    postAutherName: string;
    postText: string;
    commentText: string;
    commentAuthorName: string;
}

export interface ArticleInfo {
    title: string;
    author: string;
    postDate: string;
    contentHTML: string;
    rawText: string;
}